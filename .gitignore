# Logs
*.log
# Structured JSONL event logs (orchestrator + plugin), default logs/ dir
logs/
*.jsonl
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#testing)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-modules
node_modules/
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.eslintcache

# Tripley/Razzle toolchains
.cache

# Python compiler cache and virtualenvs
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# IDEs and editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS metadata
.DS_Store
Thumbs.db

# Local Backups
backups/

# C# build artifacts
plugin/bin/
plugin/obj/
LiftoffAutoLobby/

# Game reference assemblies (Option 1 licensing decision, docs/features/doing/
# build-release-pipeline.md section 2/2a) -- not committed; plugin/libs/README.md explains
# how to populate this directory locally. README.md itself is not matched by these patterns.
plugin/libs/*.dll
plugin/libs/*.decompiled.cs

# Runtime / generated data files
tracks_to_rotate.txt
master_tracks_list.json
bot_*.txt

# Steam Workshop publishing temp files
generator/workshop_config.json
generator/workshop_build.vdf
generator/staging/

# Docker container local config (copy of .env.example, filled in per-machine)
.env

# Runtime secrets — NEVER commit (loaded at runtime from the plugins dir / host)
liftoff_pro_username.txt
liftoff_pro_password.txt
Credentials.xml

# Private project material — kept locally only; purged from public history (2026-07-27 rewrite)
/docs/
/.claude/
/.agents/
/CLAUDE.md
/AGENTS.md
/GEMINI.md
/gemini_execution_plan.md
.obsidian/
